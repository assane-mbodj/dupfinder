process {
	executor = 'local'
	cpus = 4
    shell = ['/bin/bash', '-e']
}

process {
    withLabel: "bwa" {
        cpus = 12
    }
}

process {
    withLabel: "multithreading" {
        cpus = 8
    }
}

executor {
    name = 'local'
    cpus = 13
    memory = '64 GB'
}

nextflow.enable.dsl=1
